[package]
name = "partbooter-journal"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
partbooter-common = { path = "../common" }

[lib]
name = "partbooter_journal"
path = "src/lib.rs"

